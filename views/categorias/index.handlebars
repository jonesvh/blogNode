<div class="card">
    <div class="card-body">
        <h3>Categorias</h3>
        <hr>
        {{#each categorias}}
            <a href="/categorias/{{slug}}"><h5 class="mt-3">{{nome}}</h5></a>
        {{else}}

        {{/each}}
    </div>
</div>